<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.sipclient.gui.controller.CallController"
      spacing="30" alignment="CENTER" prefWidth="400" prefHeight="500"
      stylesheets="@../css/style.css"
      styleClass="root"
      style="-fx-background-color: linear-gradient(to bottom, #667eea 0%, #764ba2 100%);">

    <padding>
        <Insets top="40" right="40" bottom="40" left="40"/>
    </padding>

    <VBox alignment="CENTER" spacing="10">
        <Label fx:id="contactNameLabel" text="联系人"
               style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: white;"/>
        <Label fx:id="callStatusLabel" text="呼叫中..."
               style="-fx-font-size: 16px; -fx-text-fill: rgba(255,255,255,0.8);"/>
    </VBox>

    <StackPane VBox.vgrow="ALWAYS">
        <Label fx:id="avatarLabel" text="👤"
               style="-fx-font-size: 80px; -fx-text-fill: white; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.2), 10, 0, 0, 5);"/>

        <ImageView fx:id="videoView" fitWidth="320" fitHeight="240" preserveRatio="true"/>
    </StackPane>
    <Label fx:id="timerLabel" text="00:00:00"
           style="-fx-font-size: 48px; -fx-font-weight: bold; -fx-text-fill: white;"/>

    <Region VBox.vgrow="ALWAYS"/>

    <HBox spacing="30" alignment="CENTER">
        <Button fx:id="muteButton" text="🔇 静音" onAction="#handleMute"
                prefWidth="100" prefHeight="100"
                style="-fx-background-color: rgba(255,255,255,0.2); -fx-text-fill: white; -fx-font-size: 14px; -fx-background-radius: 50; -fx-cursor: hand;"/>

        <Button fx:id="hangupButton" text="📞" onAction="#handleHangup"
                prefWidth="80" prefHeight="80"
                style="-fx-background-color: #f44336; -fx-text-fill: white; -fx-font-size: 32px; -fx-background-radius: 40; -fx-cursor: hand; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.3), 10, 0, 0, 5);"/>
    </HBox>
</VBox>