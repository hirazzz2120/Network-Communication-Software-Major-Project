<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.sipclient.gui.controller.IncomingCallController"
      spacing="20" alignment="CENTER" prefWidth="350" prefHeight="250"
      stylesheets="@../css/style.css"
      styleClass="root, card">

    <padding>
        <Insets top="30" right="30" bottom="30" left="30"/>
    </padding>

    <Label text="📞 来电" styleClass="title-label"/>
    
    <VBox alignment="CENTER" spacing="10">
        <Label fx:id="callerLabel" text="未知来电" 
               style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-text-fill: -primary-color;"/>
        <Label text="正在呼叫您..." styleClass="subtitle-label"/>
    </VBox>

    <HBox spacing="20" alignment="CENTER">
        <VBox.margin>
            <Insets top="20"/>
        </VBox.margin>
        
        <Button fx:id="acceptButton" text="✓ 接听" onAction="#handleAccept"
                prefWidth="120" prefHeight="50"
                styleClass="button, success-button"
                style="-fx-font-size: 16px; -fx-background-radius: 25;"/>
        
        <Button fx:id="rejectButton" text="✗ 拒接" onAction="#handleReject"
                prefWidth="120" prefHeight="50"
                styleClass="button, danger-button"
                style="-fx-font-size: 16px; -fx-background-radius: 25;"/>
    </HBox>
</VBox>
